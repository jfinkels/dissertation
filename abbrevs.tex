\chapter*{List of abbreviations}
\singlespacing
\begin{description}
\item[$\AC$] polylogarithmic depth, unbounded fan-in circuits
\item[{\normalfont AP}] approximation-preserving
\item[$\ApxNCO$] constant factor $\NC$ approximation
\item[$\ApxPO$] constant factor polynomial-time approximation
\item[BS-BD-CVP] bounded size, bounded depth circuit value problem
\item[CSP] constraint satisfaction problem
\item[$\DTIME$] deterministic time
\item[$\DSPACE$] deterministic space
\item[$\EXP$] deterministic exponential time
\item[$\ENCAS$] efficient $\NC$ approximation scheme
\item[$\FNC$] functions computable in $\NC$
\item[$\FNCAS$] fully $\NC$ approximation scheme
\item[$\FO$] first-order formulas
\item[$\FP$] functions computable in polynomial time
\item[$\FPT$] fixed-parameter tractable
\item[$\FPTAS$] fully polynomial-time approximation scheme
\item[$\GC$] guess-and-check
\item[HDS] high degree subgraph
\item[$\L$] logarithmic space
\item[$\MaxNP$] nondeterministic polynomial-time maximization
\item[$\MaxSNP$] strict nondeterministic polynomial-time maximization
\item[$\NC$] polylogarithmic depth, bounded fan-in circuits
\item[$\NCX$] constant factor approximable $\NC$ optimization
\item[$\NCO$] $\NC$ optimization
\item[$\NCAS$] $\NC$ approximation scheme
\item[$\NEXP$] nondeterministic exponential time
\item[$\NL$] nondeterministic logarithmic space
\item[$\NNC$] nondeterministic $\NC$
\item[$\NNCO$] nondeterministic $\NC$ optimization
\item[$\NP$] nondeterministic polynomial time
\item[$\NPO$] nondeterministic polynomial time optimization
\item[$\P$] polynomial time
\item[$\p \dash k \dash \textsc{CE}$] parameterized circuit $k$-evaluation
\item[$p \dash \textsc{BHP}$] parameterized bounded halting problem
\item[$p \dash \textsc{CSat}$] parameterized circuit satisfiability
\item[$p \dash k \dash \NC^d \textsc{CSat}$] parameterized $\NC^d$ circuit satisfiability
\item[$p \dash \textsc{SCE}$] parameterized small circuit evaluation
\item[$p \dash \textsc{WCE}$] parameterized weighted circuit evaluation
\item[$\para \beta \L$] parameterized logarithmic space with $f(k) \log n$ one-way nondeterminism
\item[$\para \AC$] parameterized $\AC$
\item[$\para \EP$] parameterized polynomial-time evaluation
\item[$\para \FO$] parameterized first-order formulas
\item[$\para \NC$] parameterized $\NC$
\item[$\para \WL$] parameterized logarithmic space with $f(k) \log n$ two-way nondeterminism
\item[$\para \WNC$] parameterized $\NC$ with $f(k) \log n$ nondeterminism
\item[$\para \WNL$] parameterized nondeterministic logarithmic space with $f(k) \log n$ two-way nondeterminism
\item[$\para \WP$] parameterized polynomial time with $f(k) \log n$ two-way nondeterminism
\item[{\normalfont pb}] polynomially bounded
\item[{\normalfont PCP}] probabilistically checkable proofs
\item[$\PNC$] parameterized analog of $\NC$
\item[$\PO$] polynomial-time optimization
\item[$\poly$] polynomial functions
\item[$\polyL$] polylogarithmic space
\item[$\polylog$] polylogarithmic functions
\item[$\PTAS$] polynomial-time approximation scheme
\item[$\QP$] quasipolynomial time
\item[$\RNC$] randomized $\NC$
\item[$\SL$] symmetric logarithmic space
\item[$\SUBEXP$] subexponential time
\item[{$\W[\NC^d \SAT]$}] closure of parameterized $\NC^d$ satisfiability
\item[{$\W[\SAT]$}] closure of parameterized formula satisfiability
\item[{$\W[\P]$}] closure of parameterized circuit satisfiability
\end{description}
\doublespacing
